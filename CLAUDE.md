# ITS 経営OS — Claude Code 運用憲章

このファイルは Claude Code が毎セッションの冒頭で自動的に読み込む。
高橋が毎回指示しなくても、ここに書かれたルールに従って自律的に作業すること。
判断を都度ユーザーに聞き返さず、決めて実行する。

---

## このリポジトリの役割

`its-keiei-os` は ITS合同会社の経営OS。SES / 通信 / IT の3事業の
**戦略・スキル・コードの正本(ソース・オブ・トゥルース)** を置く場所。

- Familink(プロダクト)はライフサイクルが違うため**別リポジトリ**で管理する。
  本リポジトリとは共有メモリ層で橋渡しし、同一リポジトリに混ぜない。

---

## 最重要ルール:置き場所は「更新頻度」で決める

作業で生まれた情報は、必ず次の表に従って振り分ける。どこに保存するかを聞き返さない。

| 種類 | 置き場所 |
|---|---|
| コード / スキルファイル(.md) / アーキテクチャ判断記録(ADR) / 確定した戦略文書 / テンプレート | この git リポジトリ |
| 営業進捗・商談パイプライン / タスク状況 / 日次ログ / KPI実績 / 議事録 / 顧客メモ | Notion(MCP連携) |

- git は「差分を追う価値があり、変化が遅いもの」だけを置く。
- Notion は「頻繁に変わる、生きた業務データ」を置く。
- 業務データを git にコミットしようとすると `.claude/hooks` のフックが
  自動でブロックする。ブロックされたら Notion 側へ回すこと。

---

## Notion運用プロトコル（厳守）

**前提：本プロトコルは Notion MCP 接続が必須**（egress許可 `mcp.notion.com`）。未接続の間は実行できないため、まず接続を整える。

### 起動時
1. 本 CLAUDE.md を読む
2. Notion MCP へ接続する
3. 次を確認し、前回状態を取得してから着手：**営業管理DB／案件管理DB／人材管理DB／TODO管理DB／AI引継ぎログ**

### 更新操作（順序固定）
**①search → ②read → ③差分確認 → ④update / append**
- 絶対禁止：確認なし新規作成／重複作成（会社名・案件名・人材名をキーに照合）／既存情報の削除。
- 存在すれば既存ページを update＋活動履歴を append＋最新日時を更新。**存在しない場合のみ create**。
- 営業履歴フォーマット（append専用・既存は絶対に削除しない）：
  ```
  YYYY/MM/DD
  ・実施内容
  ・次回アクション
  ・期限
  ```

### セッション終了（引継ぎ）
作業終了前に必ず **AI引継ぎログ** へ追記する：
```
## yyyy/mm/dd hh:mm
完了:
-
途中:
-
次回:
-
問題:
-
```
次回起動時は、このログを最初に読む。

> 注意：現行の git→Notion ミラー同期は「全消し→再生成」のため append と両立しない。append型（営業履歴・引継ぎログ）はミラー対象に置かず、MCP直接管理ページに置く。

---

## 正本の地図は1枚だけ

- ナビゲーションの入口は **本ファイル** と **Notion マスターページ** の2点のみ。
- リンクを無秩序に増やさない。新しい文書は、必ずどちらかの索引から
  1ホップで辿れる場所に置く。
- 索引自体は中身を持たず「どこに何があるか」を指すだけにする。

---

## セッションはワークストリーム単位でスコープを切る

1セッション = 1ワークストリーム。混ぜない。

- **ITS経営**(本OSの構築・戦略)
- **Familink**(プロダクト開発)
- **営業**(進捗・商談)

長大な万能スレッドにしない。文脈が膨らむほど後半の応答品質が落ちるため。

---

## デスクトップ / モバイルの役割分担

- 重い実装(多ファイル編集・テスト実行・リファクタ)は**デスクトップの Claude Code**。
- モバイルは**キャプチャ・レビュー・リモート起動**に限定する。

---

## セッション終了プロトコル(毎回、自律的に実行)

セッションを終える前に、その回で確定した内容を上の振り分け表に従って反映する:

1. コード / スキル / 確定戦略 → コミット(簡潔な日本語メッセージ)。
2. 営業進捗 / タスク / KPI / 議事録 → Notion に MCP で書き込み。
3. 新しい恒久ルールが生まれたら、本 CLAUDE.md に追記する。
   ただし全体を **50〜200行** に保ち、肥大化させない。詳細は別ドキュメントに逃がしリンクする。

「どこに保存するか」をユーザーに聞き返さず、自分で判断して実行する。

---

## 実装方法論(踏襲)

DBスキーマ設計 → 画面レイアウト → 実装 → テスト の順。毎ターン状況を簡潔に報告する。
プレースホルダ機能を作らない。自己完結し即実行できる成果物を出す。
作業途中に確認のために止まらない。

---

## 設計原則(全プロジェクト共通)

装飾的な絵文字を使わない / 線画アイコン / 落ち着いた配色 / 余白を広く /
簡潔な日本語コピー / 全年齢が使えるUI。

# 要員定義（自社プロパー）：KN／PMO補佐・プロジェクト推進支援

要員ID：`KN_PMOサポート`　／　登録日：2026-07-12　／　所属：**ITS合同会社（弊社プロパー）**

> **向き（重要）**：これは「**ITSの要員（KN）に案件を当てる**」逆方向マッチング用の定義。
> 従来の `案件_*.md`（ITSの案件にBP要員を当てる）と対になる。営業の型は `../../skills/ses-sales/SKILL.md`、
> 生成の実体は `make_offer_draft.py`（`reply-template-offer.txt` ＋ 下の MAIL-BLOCK を差し込み）。
> **送信は必ず代表が手動**（AIは下書きまで）。スキルシート原本は個人情報のためgit非コミット（`data/skillsheets/`＝gitignore）。

## 1. 要員サマリー

- **氏名**：KN（30歳・女性）
- **所属**：弊社所属（ITS合同会社プロパー）
- **希望単価**：48万円（応相談）
- **参画時期**：即日〜
- **最寄駅**：大井町駅
- **並行状況**：提案中
- **勤務**：出社・リモート相談可能／土日含む週5日稼働可
- **スキルシート原本**：`../../data/skillsheets/KN_スキルシート.pdf`（gitignore・提案時に添付）

## 2. 希望条件（当てにいく案件の型）

- PMO補佐／PMサポート／プロジェクト推進支援
- IT事務／プロジェクトアシスタント
- サービス導入支援
- サポート案件
- バックオフィス支援／営業事務
- 役員秘書・経営サポート

## 3. 経験概要（スキルシートより要約）

営業職・サービス導入支援・役員秘書業務を中心に経験。

- **LINE Pay株式会社**：法人向けキャッシュレス決済サービスの導入支援。新規加盟店への提案、
  課題ヒアリング、導入スケジュール管理、契約手続き・申込進捗管理、関係部署との調整。
  入社5ヶ月で営業部門最優秀賞を受賞。
- **役員秘書（代表取締役付き）**：スケジュール管理、会議調整、資料作成、議事録作成、出張・各種手配、
  情報収集・リサーチ、助成金/補助金の情報整理。複数タスクの優先順位付けと正確な情報整理。
- **法人営業支援／インサイドセールス**：法人アプローチ、商談日程調整、顧客情報・営業進捗管理、
  問い合わせ対応、資料作成。
- **フリーランス事業運営／顧客サポート**：顧客ヒアリング、契約管理、品質改善、リピート対応。

営業で培った高いコミュニケーション力に加え、関係者調整・タスク管理・資料作成が強み。
PMO補佐・プロジェクト推進支援領域での活躍が期待できる。

## 4. マッチング基準（`make_offer_draft.py` の keyword 判定に渡す材料）

> 逆方向は**案件配信の本文（案件概要）**でマッチする（要員に添付は不要）。`scoring.md` の共通軸で見つつ、
> KN固有の「当たる/外れる」語をここで定義する。総合判断は代表（AIは下書きまで）。

**加点（KNが刺さる語）：**
`PMO` / `PMO補佐` / `PMサポート` / `PM補佐` / `プロジェクト推進` / `プロジェクトアシスタント` /
`PJアシスタント` / `アシスタント` / `IT事務` / `事務` / `サービス導入` / `導入支援` / `導入サポート` /
`オンボーディング` / `サポート` / `ヘルプデスク`（非技術系） / `バックオフィス` / `営業事務` / `秘書` /
`進捗管理` / `課題管理` / `タスク管理` / `資料作成` / `議事録` / `調整` / `問い合わせ対応` / `カスタマーサクセス`

**減点・要確認（KNが不適合になりやすい技術専任語）：**
`設計` / `構築` / `開発` / `プログラ` / `コーディング` / `Java` / `Python` / `インフラ構築` /
`ネットワーク設計` / `サーバ構築` / `SE（開発）` / `エンジニア（実装）` / `DBA` / `保守開発`
→ これらが中心で PMO/事務/サポート要素が無い案件は「技術専任・要確認」でフラグ（自動除外はしない・代表判断）。

**単価整合：** KN希望48万（応相談）。案件上限が **48万＋粗利¥8万＝56万以上** なら整合。
それ未満は「単価要調整」でフラグ（応相談のため即除外はしない）。

## 5. ガードレール（この要員に固有）

- **スキルシート原本（PDF）は個人情報**。gitにコミットしない（`data/skillsheets/`＝gitignore）。
  自動化で添付する場合の正本は Notion添付/Drive（`skillsheet-intake.md`）。当面は**代表が送信時に手動添付**でも可。
- スキルシートに嘘は書かない（強調順の最適化のみ）。年齢・性別など属性は本文の要員サマリーに記載する範囲に留め、
  案件側の属性条件（年齢/性別/国籍など）で**自動選別しない**（法令グレー・代表判断）。
- 単価は「48万・応相談」。案件単価が大きく下振れする場合は代表に確認（生活・粗利ライン）。

## 6. メール貼付用・要員サマリー（提案下書きにそのまま挿入）

`make_offer_draft.py` が下の `MAIL-BLOCK-START/END` に挟まれた本文を**そのまま**提案下書きに差し込む（LLMで書き換えない）。
文面を直したいときはこのブロックを編集すれば、次回以降の全提案に反映される。

<!-- MAIL-BLOCK-START -->
【要員サマリー】

■氏名
KN（30歳・女性）

■希望単価
48万円（応相談）

■参画時期
即日～

■最寄駅
大井町駅

■所属
弊社所属

■並行状況
提案中

■希望条件
・PMO補佐／PMサポート
・IT事務／プロジェクトアシスタント
・サービス導入支援
・サポート案件
・バックオフィス支援
※出社・リモート相談可能

■経験概要
営業職・サービス導入支援・役員秘書業務を中心に経験しております。

LINE Pay株式会社では、法人向けキャッシュレス決済サービス導入支援に従事し、新規顧客への提案、課題ヒアリング、導入までの進捗管理、関係部署との調整業務を担当しておりました。

また、代表取締役付き秘書として、スケジュール管理、会議調整、資料作成、議事録作成、各種手配業務、情報収集など経営層の業務支援を経験しております。

営業経験で培った高いコミュニケーション力に加え、関係者調整・タスク管理・資料作成を得意としており、PMO補佐やプロジェクト推進支援領域で活躍が期待できます。

■主な技術
・進捗管理／課題管理
・タスク管理
・関係者調整
・会議調整／議事録作成
・資料作成
・業務改善サポート
・サービス導入支援
・顧客折衝

【使用ツール】
Excel
Word
PowerPoint
Google Workspace
各種チャットツール
Web会議ツール

■備考
・コミュニケーション能力良好
・法人顧客折衝経験あり
・サービス導入推進経験あり
・役員クラスとの調整経験あり
・主体性、キャッチアップ力◎
<!-- MAIL-BLOCK-END -->
